spring:
 datasource:
  url: jdbc:mysql://localhost:3306/auth
  username: root
  password: admin
 jpa:
  hibernate:
   ddl-auto: update
 application:
  name: mcsv-auth
 cloud:
  config:
   enabled: true
   uri: http://localhost:8081